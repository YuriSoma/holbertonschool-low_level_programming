#include "main.h"

/**
 * _strspn - 
 * @s: the string
 * @c: letter to be located
 *
 * Return: s after locating c.
 */
unsigned int _strspn(char *s, char *accept)
{
	int i = 0;

	for (i = 0; s[i] != c; s++)
	{
		if (s[i] == '\0')
			return (0);
	}
	return (s);
}

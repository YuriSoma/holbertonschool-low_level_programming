#include <ctype.h>

/**
 * _isalpha - checks if the character is an alpha
 * @c: The character to be check
 *
 * Return: On success 1.
 * On error, 0 is returned.
 */
int _isalpha(int c)
{
	if (!_isalpha(c))
		return (0);
	return (1);
}

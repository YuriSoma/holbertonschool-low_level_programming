#include "main.h"

/**
 * add - prints tale 9
 * @a: param for first arg
 * @b: param for second arg
 * Description: Function that adds two integers and returns the result
 * Return: returns the addition of the two args
 */
int add(int a, int b)
{
	return (a + b);
}

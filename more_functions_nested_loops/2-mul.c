#include "main.h"

/**
 * mul -  multiply 2 digits.
 * @a: first digit.
 * @b: second digit.
 *
 * Return: the result of the multiplication.
 */
int mul(int a, int b)
{
	int r = a * b;
	return (r);
}
